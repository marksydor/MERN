const express = require('express');
const router = express.Router();
const coments = require('../models/coments.js');

router.post('/coment', async (req, res) => {
	let user_id = 1;
	
});

module.exports = router;